---
- name: Display all encrypted data
  hosts: all
  gather_facts: false
  tasks:
    - name: Variables
      ansible.builtin.debug:
        var: item
      loop:
        - "{{ bot_api_key }}"
        - "{{ tb_api_key }}"
        - "{{ owner }}"
        - "{{ redis_pass }}"
